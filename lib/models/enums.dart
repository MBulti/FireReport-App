enum FilterStatus { all, open, inProgress, done, assignedToMe, createdByMe, unread }

enum ReportState { open, inProgress, done }

enum RequestType { normalUniform, operationalUniform }

enum RoleType { unset, admin, wehrfuerung, loeschgruppenfuerung, geraetewart, user }

enum UnitType { unset, kilver, roedinghausen, bieren, schwenningdorf, jfSued, jfNord, kinderfeuerwehr }